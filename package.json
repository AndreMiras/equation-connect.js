{
  "name": "equation-connect",
  "version": "2.2.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "equation-connect": "dist/cli.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:emulator": "firebase emulators:exec --project demo-equation-connect --only auth,database \"yarn test:emulator:run\"",
    "test:emulator:run": "vitest run --config vitest.emulator.config.ts",
    "test:package": "node scripts/test-packed-package.mjs",
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "build": "tsc",
    "cli": "tsx src/cli.ts"
  },
  "files": [
    "/dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AndreMiras/equation-connect.js.git"
  },
  "author": "Andre Miras",
  "license": "ISC",
  "engines": {
    "node": ">=22"
  },
  "bugs": {
    "url": "https://github.com/AndreMiras/equation-connect.js/issues"
  },
  "homepage": "https://github.com/AndreMiras/equation-connect.js#readme",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "firebase-tools": "15.28.2",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0",
    "typedoc": "^0.28.19",
    "typescript": "^5.6.3",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "firebase": "^12.18.0"
  },
  "optionalDependencies": {
    "commander": "^14.0.3"
  }
}
